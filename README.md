# RPG Game Project

## 运行方法

确保已安装 `uv`。

### 本地桌面版运行

```bash
uv run main.py
```

### 浏览器 Web 版运行 (Pygbag)

第一次运行需要安装依赖：

```bash
uv add pygbag
```

启动本地 Web 服务器预览：

```bash
uv run pygbag main.py
```

然后在浏览器访问 `http://localhost:8000`。

